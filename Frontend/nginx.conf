server {
    listen 80;
    server_name _;

    # Let's Encrypt validation path
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    root /usr/share/nginx/html;
    index index.html;

    # Serve React static chunks/pages correctly
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Reverse proxy /api to Django Backend
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# NOTE: HTTPS server block is intentionally omitted.
# After provisioning SSL certificates with Certbot, add:
#
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#     ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#     ...same location blocks as above...
# }
